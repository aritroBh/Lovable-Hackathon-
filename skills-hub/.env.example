# Copy to .env and fill in (loaded automatically by dev-api-server)
TAVUS_API_KEY=
TAVUS_PERSONA_ID=
TAVUS_REPLICA_ID=          # required unless persona has default_replica_id in PALmaker
# TAVUS_CALLBACK_URL=        # optional webhook for conversation lifecycle
# TAVUS_PUBLIC_BASE_URL=     # only if Tavus must reach your upload URL during replica training
